# OOFPP Habit Tracker (CLI)

A basic habit tracker backend in Python (OOP + FP) with persistence and analytics.

## Requirements
- Python 3.7+

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
